#include <bits/stdc++.h>
using namespace std;

#define flush fflush(stdout)

int main() {
    string in;
    while (getline(cin, in)) {
        printf("NO\n");
        flush;
    }
    
    return 0;
}